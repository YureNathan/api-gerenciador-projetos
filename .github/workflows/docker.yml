name: CI/CD Docker build and Test

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: ✅ Login no Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🏷️ Definir tag da imagem
        run: |
          # Define a tag da imagem e a exporta para os próximos passos
          # Isso garante que seu docker-compose.yml usará a imagem correta
          echo "DOCKER_IMAGE_TAG=${{ secrets.DOCKER_USERNAME }}/gerenciador_projeto:latest" >> $GITHUB_ENV

      - name: 🚀 Buildar e carregar imagem localmente
        uses: docker/build-push-action@v4
        with:
          context: .
          push: false # NÃO faz push ainda
          load: true  # CARREGA no daemon local do Docker
          tags: ${{ env.DOCKER_IMAGE_TAG }} # Usa a tag definida no passo anterior

      - name: 🐋 Subir ambiente com Docker Compose (para teste)
        # Usa 'docker compose' (v2) e a variável DOCKER_IMAGE_TAG
        # O 'depends_on.condition: service_healthy' fará ele esperar o DB ficar pronto
        run: docker compose up -d

      - name: 🩺 Verificar status dos contêineres pós-healthcheck
        run: |
          echo "Ambiente iniciado. Verificando status..."
          docker compose ps
          
          echo "--- Logs do Banco de Dados (postgres-db) ---"
          docker compose logs postgres-db
          
          echo "--- Logs da API (api-spring) ---"
          docker compose logs api-spring

      # Você pode adicionar mais testes aqui (ex: curl)

      - name: 🚀 Fazer Push da imagem para o Docker Hub
        if: success() # Só faz push se os passos anteriores (incluindo o 'up') passarem
        run: docker push ${{ env.DOCKER_IMAGE_TAG }}

      - name: 🛑 Parar os contêineres
        if: always() # Sempre executa, mesmo se um passo anterior falhar
        run: docker compose down